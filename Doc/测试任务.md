# STM32F407 测试任务

## 1. 启用

1. 将 `Test/test_config.h` 中的 `PROJECT_TEST_TASKS_ENABLE`设为`1U`。
2. 确认需要的测试源文件参与当前 Target 编译。
3. 在 `APP/app_task_config.h` 临时包含 `test.h`并替换任务表。
4. 测试完成后恢复正式任务表和测试开关。

基础任务：

```c
#define APP_SCHEDULER_TASK_LIST_DEFINE()                         \
Task_t task_list[] = {                                           \
    { AppDiagnostics_HeartbeatUpdate, 10U, 0U },                 \
    { AppTask_BSP_Background,     1U, 0U },                      \
};                                                               \
const uint8_t TASK_NUM =                                         \
    (uint8_t)(sizeof(task_list) / sizeof(task_list[0]))
```

`AppDiagnostics_HeartbeatUpdate()`才是运行指示任务。`Test_GPIO_Toggle()`只是普通 GPIO 翻转测试。

## 2. 常用组合

### 按键

```c
    { Key_Update,      10U, 0U }, \
    { Test_Key_Update, 10U, 0U }, \
```

### E220 双向通信

```c
    { Test_E220_Link_Update, 10U, 0U }, \
```

保留 `AppTask_BSP_Background()`。该测试直接刷新 LCD/OLED，不要同时注册其他显示页面。

### K210 单数字

```c
    { K210_Comm_Update,                 5U, 0U }, \
    { Test_K210_SingleDigitCommUpdate, 10U, 0U }, \
```

其他 K210 测试也必须先注册 `K210_Comm_Update()`。

### 姿态

```c
    { Sensor_Update,        1U, 0U }, \
    { Test_Attitude_Update, 10U, 0U }, \
```

### 底盘和动作

常见前置任务：

```c
    { Key_Update,      10U, 0U }, \
    { Encoder_Update,  10U, 0U }, \
    { Motion_Update,   10U, 0U }, \
    { Chassis_Update,  10U, 0U }, \
```

只保留当前测试需要的任务，避免多个测试同时控制电机或显示。

## 3. 入口速查

| 类别 | 入口 |
| --- | --- |
| 基础 GPIO | `Test_GPIO_Toggle`、`Test_StatusLight_Update`、`Test_Buzzer_Update`、`Test_PWM_Ramp` |
| 按键和中断 | `Test_Key_Update`、`Test_EXTI_Init`、`Test_EXTI_Log` |
| UART / I2C | `Test_UART_Echo`、`Test_UART_Stats`、`Test_I2C_Scan` |
| 编码器 | `Test_Encoder_Log`、`Test_DrvEncoder_Log`、`Test_CountPerRev_Update` |
| 灰度和循迹 | `Test_Gray4051_Update`、`Test_Gray4051_Log`、`Test_LineCmd_Update`、`Test_LineCmd_Log` |
| 路线 | `Test_RouteCmd_Update`、`Test_RouteLog` |
| 电机和底盘 | `Test_MotorCmd_Update`、`Test_MotorCmd_Log`、`Test_ChassisCmd_Update`、`Test_ChassisWatchdog_Update`、`Test_ChassisCmd_Log` |
| 动作 | `Test_MotionCmd_Update`、`Test_MotionCmd_Log` |
| 传感器 | `Test_VL53L1X_Update`、`Test_HX711_Update`、`Test_ICM20948_Update`、`Test_ICM20948_Mag_Update`、`Test_Attitude_Update` |
| 显示 | `Test_LCD_Ascii_Update`、`Test_OLED_Ascii_Update`、`Test_AsyncDisplay_Update` |
| E220 | `Test_E220_Link_Update` |
| K210 | `Test_K210_CommUpdate`、`Test_K210_DigitCommUpdate`、`Test_K210_RoadCommUpdate`、`Test_K210_VisionCommUpdate`、`Test_K210_RoadProfileUpdate`、`Test_K210_SingleDigitCommUpdate` |
| 其他 | `Test_TaskFSM_Log`、`Test_DriveProfile_Update` |

最终入口以 `Test/test.h` 为准。
