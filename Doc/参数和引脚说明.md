# 参数和引脚说明

> 本文按当前项目源码校正。当前 `Doc` 目录未发现对应的 Word 原文件；若后续恢复 Word 文件，必须同时检查本文和 `说明文档.md`。

## 一、车辆运动控制参数：Algorithm/control_config.h

底盘开环PWM映射、灰度循迹PD和动作库转向P的默认参数集中在`Algorithm/control_config.h`。`APP/chassis.h`、`APP/motion_action.h`和`Algorithm/line_track.h`只保留接口和数据结构，不再放调参宏。

底盘已经取消车轮速度PID。上层仍使用`cps`形式的目标值，底盘按固定比例换算PWM；编码器只用于反馈显示、里程和定距。实际控制链如下：

`灰度位置误差 → LineTrack PD生成linear/turn（cps）→ left=linear-turn、right=linear+turn → 固定比例换算 → 电机PWM（千分比）`

当前换算关系为`PWM=目标cps×CONTROL_CHASSIS_PWM_MAX_PERMILLE/CONTROL_CHASSIS_TARGET_MAX_CPS`，即`5000 cps→800‰ PWM`。`Chassis_Update()`和`LineTrack_Compute()`通常每10 ms调用一次；灰度PD和命令步进按“每次调用”计算，改变周期后需要重新验证`CONTROL_LINE_KD`和各项步进参数。

### 1.0 灰度循迹安装方向

```c
#define CONTROL_LINE_DIRECTION_REVERSE 1U
```

- `0U`：保持当前Driver输出的左右方向；规范化后线路在左时误差为负、线路在右时误差为正。
- `1U`：传感器整体旋转180度、循迹左右相反时使用；算法会同时反转位置误差、`black_mask`位序和左右分支类型。
- 该开关不重排逐路阈值。`LINE_DETECT_DEFAULT_THRESHOLD_0`～`_7`始终与Driver输出的物理通道一一对应，旋转传感器后不要手工倒序阈值；若安装高度或角度变化，应重新执行黑白标定。
- `DRV_GRAY_MCU_INDEX_REVERSE`和`DRV_GRAY_4051_INDEX_REVERSE`用于处理底层通道接线/编号。只改变传感器安装朝向时优先修改`CONTROL_LINE_DIRECTION_REVERSE`，不要同时修改两层反向开关。

### 1.1 底盘开环PWM映射

> 车轮速度PID已经从`APP/chassis.c`执行链中移除。旧版PID参数、调参步骤和故障表不再适用于当前源码；当前有效底盘参数只有`CONTROL_CHASSIS_PWM_MAX_PERMILLE=800`和`CONTROL_CHASSIS_TARGET_MAX_CPS=5000`。编码器仍保留，但不会修正PWM。

#### 大白话解释

每个轮子都有一个独立的速度控制器。它做的事很简单：**你告诉它"目标转多快"（比如 2000cps），它自动调节电机 PWM，让轮子实际转速尽量接近目标**。PID 三个字母代表三种调节手段：

- **P（比例）**：误差越大、油门越大。好比你看车速表慢了就踩油门，差得越多踩得越狠。
- **I（积分）**：把过去的误差慢慢累加起来，"你慢了这么久，我再帮你多补一点"，用来消除"明明一直在踩油但车速就是差一点"的情况。
- **D（微分）**：检测误差是不是在快速变化，变化快的时候提前收油或刹车，防止冲过头。

FL、FR（4WD 时还包括 RL、RR）各有一份独立的 PID 状态，但当前四轮共用下面同一组增益。PID 计算在每个 10ms 周期执行一次，输出直接作为电机 PWM（范围 -1000‰ ~ +1000‰）。

#### 当前实测参数结论（2026-07-18）

| 参数组合 | 按当前离散算法的实际贡献 | 结论 |
| --- | --- | --- |
| `KP=1.2` | 速度误差 100 cps 时，比例项输出 120‰ PWM | 比旧值更有力，但没有激进到小误差就打满输出，适合作为实测基线 |
| `KI=0.08`、`INTEGRAL_MAX=800` | 积分输出最多为 `0.08×800=64‰`；误差持续为100 cps时约8个10 ms周期达到积分限幅 | 积分建立较快但最大补偿受控，可消除小稳态误差，积分拖尾风险有限 |
| `KD=0.02` | 相邻周期误差变化100 cps时，微分项仅输出2‰ | 微分作用很轻，若实测无高频噪声可保留；它不会承担主要制动作用 |
| `OUTPUT_MAX=800` | PID输出限制在±800‰ | 保留20%占空比余量，合理 |
| `TARGET_MAX_CPS=5000` | 只限制底盘最终左右目标；当前按键测试最高3000、动作库最高2500 | 作为总上限合理，但不代表电机已经实测能稳定达到5000 cps |

> **结论：这组参数在代码计算关系上合理，且车轮速度内环已经完成实测，可作为当前基线。** 后续动作库和循迹调试先不要同时修改这组PID；只有落地带载后再次出现周期振荡、明显过冲、停车拖尾或长期稳态误差时，才回到本节复查。

#### 🔑 关键：怎么区分"机械抖动"和"PID 震荡"

新手最容易搞混的两个东西。**车轮抖、车在振动、声音大，不等于 PID 震荡。**

| | 🔴 PID 震荡（真问题） | 🟢 机械抖动（正常，不用管） |
| --- | --- | --- |
| **编码器反馈** | 数值周期性来回大幅跳动（比如目标 1500，反馈在 1100→1900→1200→1800 之间摆动） | 数值在目标附近小幅波动（比如 1480~1520） |
| **电机声音** | 周期性变调——"呜~呜~呜~"，节奏感很强 | 持续的高频啸叫、齿轮咔咔声、或车身共振嗡嗡声 |
| **PWM 输出**（看日志） | 在正负之间大幅摆动（+400→-200→+500→-300） | 在某个值附近小幅波动（比如一直在 280~320 之间） |
| **轮子转动** | 肉眼可见的忽快忽慢 | 转速基本均匀，但车身/桌面跟着一起震 |

> **判断方法**：不要用眼睛看、用耳朵听来判断。**看 200ms 串口日志里的编码器反馈值和 PWM 输出值**。反馈值稳不稳定、PWM 有没有大幅正负跳变，才是唯一的判断标准。

#### 各参数详解

| 参数 | 当前实测值 | 当前结论 | 通俗解释 |
| --- | ---: | ---: | --- |
| `CONTROL_CHASSIS_PWM_MAX_PERMILLE` | `800` | **当前有效** | 最大开环PWM为约80%占空比。 |
| `CONTROL_CHASSIS_TARGET_MAX_CPS` | `5000` | **保留为总限幅** | **"最终左右轮目标上限"**。当前按键、动作和循迹命令均低于5000；若以后直接请求5000，仍需单独确认电机能力和输出是否长期饱和。 |

> 💡 **理解输出限幅 vs 目标限幅**：`OUTPUT_MAX` 限制的是 **PWM油门**（最多 80% 占空比），`TARGET_MAX_CPS` 限制的是 **目标转速**（最多让轮子转多快）。两者管的不是一回事——把 OUTPUT_MAX 改大不能让轮子转更快（轮子转速由 KV 值和电压决定），只能让电机更有劲（扭矩更大）。

#### 五按键底盘开环测试参数

手动切回五按键底盘开环测试时，使用`Driver/test.c`中的测试专用宏：

| 宏 | 当前值 | 按键行为 |
| --- | ---: | --- |
| `TEST_CHASSIS_INITIAL_LINEAR_CPS` | `1600` | KEY1：从该线速度前进，并重置加速档位 |
| `TEST_CHASSIS_LINEAR_STEP_CPS` | `200` | KEY2：每个消抖后的按下沿增加一次 |
| `TEST_CHASSIS_LINEAR_MAX_CPS` | `4800` | KEY2：达到该值后不再增加；低于底盘总限幅5000 cps |
| `TEST_CHASSIS_TURN_CPS` | `1600` | KEY3/KEY4：以正/负该转向速度原地左转/右转 |

KEY5 调用 `Chassis_Stop()`，因此目标速度、PID 累积状态和电机输出都会清零。按住 KEY2 不会连续增加速度；松开后再次按下才会增加 200 cps。上电后或 KEY5 停止后，KEY2 会被忽略，必须先按 KEY1 才能进入前进和加速流程。

#### 以后更换电机、负载或10 ms任务周期时的重新调试步骤

当前内环已经完成实测，以下步骤作为重新调参方法保留，进入动作库和循迹测试时不需要重复执行，也不要一边调外层动作一边改内环PID。

**第 0 步：确认硬件接线正确**
- 悬空车轮，给一个小的正目标速度（比如 500cps），用手摸轮子确认转动方向和编码器反馈的正负一致。FL 和 FR 不能接反——一个正转一个反转会让车原地打转。

**第 1 步：找到真正的震荡边界（看日志，别看轮子）**
- 设置 `KI=0`、`KD=0`。
- 固定目标速度（比如 1500cps），从 **KP=1.0** 开始，每次加 **0.2~0.3**。
- **每改一次，看 200ms 串口日志的输出**，观察编码器反馈值和 PWM 输出值。
- 真正的震荡：**反馈值周期性大幅跳动**（比如目标 1500，反馈在 1000↔2000↔1100↔1900），且 PWM 输出在正负之间剧烈摆动。
- 轮子抖、车身震、声音大但编码器数值稳定 → **不是震荡，是机械振动，可以继续加 KP**。
- 记下编码器数值开始大幅跳动时的 KP 值（临界增益 Ku），安全 KP = Ku × 0.7。
- 比如你试到 KP=2.0 时反馈值开始乱跳，则安全 KP ≈ 1.4。如果一直试到 3.0 都不跳，说明电机阻尼很大，KP 取 2.0~2.5 就够了。

**第 2 步：加 KI，消除稳态误差**
- 保持 KD=0，KP 用上一步确定的安全值（Ku × 0.7）。
- 先把 `INTEGRAL_MAX` 改大到 **800**（给积分留够空间）。
- 从 **KI=0.03** 开始，每次加 0.01。用同样的目标速度测试。
- **看日志**：反馈值是否在几秒内从"差一截"逐渐逼近目标值。正常情况误差会收敛到很小。
- 停止加 KI 的信号：起步时反馈值明显超过目标再掉回来（过冲），或者停车后速度迟迟降不到零。
- 如果 0.03 就过冲，把 KI 降到 0.02 或把 KP 降到上一步的 0.6 倍再试。

**第 3 步：确认是否真的需要 KD**
- 大多数小型编码器电机不需要 KD。
- 只有当你看到以下现象时才加：**明显过冲**（比如目标 1500，起步时冲到 1800+），且反馈数据比较干净（编码器读数不会乱跳几个计数）。
- 如果加了 KD 后电机发出"咔咔"的高频噪声，说明编码器噪声被微分放大了——马上退回 KD=0。
- 真需要的话，从 KD=0.01 开始，不要超过 KP 的 1/10。

**第 4 步：路测验证**
- 把车放地上跑（不要悬空），观察实际循迹效果。
- 重点关注：直道时左右轮速度是否一致、加速时是否有一侧明显掉速、过弯时内侧轮会不会停转。
- 如某侧轮子经常掉速，**先查那颗电机的编码器方向和机械连接**，不要急着改 PID。

#### 快速参考：常见问题和怎么改

| 现象 | 优先改什么 | 怎么改 |
| --- | --- | --- |
| 编码器反馈值周期性大幅跳动，PWM 正负乱跳 | KP 太大（真震荡） | 减小 KP 到当前值的 0.7 倍 |
| 车轮抖、车身震、声音大，但编码器数值稳定 | 机械振动，不是 PID 问题 | 不用改 PID；检查车轮动平衡、齿轮啮合、固定螺丝 |
| 空载转速正常，放地上就跑不动 | 先看PWM是否长期达到800‰ | 若已饱和先查供电、电机和负载；未饱和再以0.1为步长微调KP |
| 轮子速度始终比目标慢一截（比如目标2000，稳定在1600） | 先看PWM和误差是否稳定 | 当前已有KI；若PWM未饱和，可小幅调整KP或以0.01为步长调整KI，不要直接跳到大增益 |
| 起步时反馈值猛冲超过目标再掉回来 | KI 太大或 INTEGRAL_MAX 太大 | 从当前KI=0.08开始每次减0.01；仍有拖尾再适当减小INTEGRAL_MAX |
| 停车命令发出后轮子还要转一会儿 | KI 太大 + 积分没清零 | 检查 `Chassis_Stop()` 是否调用了 `PID_Reset()`；同时减小 KI |
| 两侧轮子行为明显不同 | 先查硬件 | 对调电机/编码器排查；确认齿轮啮合和轴承顺滑度 |

### 1.2 灰度位置 PD——让车沿着线走

#### 大白话解释

灰度PD决定“车该往哪走、走多快”，底盘再把目标固定换算为PWM。它做的事：

1. 8 路灰度传感器看到黑线的位置，算出一个误差值（-3500 到 +3500，负数=线在左边，正数=线在右边）
2. 用 PD 公式算出”该拐多少”：**转向量 = -(KP × 误差 + KD × 误差变化量)**
3. 根据误差大小自动减速（偏离越多越慢）
4. 把计算结果（linear + turn）发给底盘差速模型并固定换算为电机PWM

完整数据流：

```
8路灰度传感器（模拟ADC值，0~4095）
  → LineDetect（与阈值比较→黑白二值→加权平均→误差 -3500~+3500）
    → LineTrack PD（误差→转向量 + 自适应速度）
      → Chassis_SetSpeed(linear, turn) → 固定比例换算 → PWM
```

> 调试时先确认左右电机方向、开环PWM和编码器方向，再调灰度PD。

#### 第一步：切换任务表，进入循迹测试模式

当前任务表是”五按键动作库测试”，需要切换到”循迹测试”。打开 [app_task_config.h](APP/app_task_config.h)，把 **当前启用的那组** 注释掉，把下面这组取消注释：

```c
// 循迹测试任务表（KEY1启动循迹，KEY4停止）
{ Test_GPIO_Toggle,        10U, 0U },
{ AppTask_BSP_Background,   1U, 0U },
{ Key_Update,              10U, 0U },
{ Sensor_Update,            1U, 0U },
{ Encoder_Update,          10U, 0U },
{ Test_RouteCmd_Update,    10U, 0U },   // KEY1启动/KEY4停止循迹
{ LineTrack_Update,        10U, 0U },   // 灰度采集→线检测→PD计算→底盘
{ Motion_Update,           10U, 0U },   // 动作库（路线可能用到）
{ Chassis_Update,          10U, 0U },   // cps目标固定换算为PWM
{ Test_RouteLog,          200U, 0U },   // 激活路线LCD/OLED调试页，不发送串口日志
{ LCD_Update,              20U, 0U },   // 非阻塞绘制，页面数据约200ms刷新
{ OLED_Update,             20U, 0U },   // OLED路线A/B页面刷新
```

`Test_RouteCmd_Update` 的按键行为：KEY1 启动循迹，KEY4 停止。没有转速加减功能，循迹速度由参数决定。

#### 第二步：确认灰度传感器能正常看到线

**这一步比调 PD 更重要。传感器看不到线，PD 再好也没用。**

0. **悬空所有车轮（架起来）**，防止车突然冲出去。
1. 烧录循迹任务表，上电，用一个黑胶带/黑线放在传感器下方。
2. 查看路线LCD/OLED调试页的`RAW`和`FIL`。`Test_RouteLog`只负责激活页面，`LCD_Update`/`OLED_Update`负责对应屏幕刷新，不再发送串口日志：

| 你把黑线放在哪 | raw_error 应该是 | 说明 |
| --- | ---: | --- |
| 正中间（传感器 3~4 之间） | 接近 0（±200 以内） | ✅ 正常 |
| 最左边（传感器 0） | 接近 -3500 | ✅ 正常 |
| 最右边（传感器 7） | 接近 +3500 | ✅ 正常 |
| 左右移动黑线 | 数值平滑跟随变化 | ✅ 正常 |
| 黑线在任意位置都显示 LOST 或 error 不变 | — | ❌ 阈值不对，需要校准 |

3. 如果 error 完全不跟踪黑线位置，**先校准阈值**。方法：
   - 把传感器放白底上（不要有黑线），调用 `LineDetect_CaptureWhite()`
   - 把黑线放到传感器正下方，调用 `LineDetect_CaptureBlack()`
   - 调用 `LineDetect_MakeThresholdFromWhiteBlack()`
   - 或者在串口用 `p` 命令查看当前 8 路 ADC 值，手动改 `LINE_DETECT_DEFAULT_THRESHOLD_0~7`

#### 第三步：PD 调参（核心）

PD 公式：**转向 = -(KP × 误差 + KD × (误差 - 上次误差))**

其中误差范围 -3500~+3500，负号的意思是”线在左边就往左拐”。

| 参数 | 通俗解释 | 怎么调 |
| --- | --- | --- |
| **`CONTROL_LINE_KP`** | **“偏了多少就拐多少”**，误差乘KP后形成主要转向量。 | 弯道拉不回来时小步增加；出现蛇形或边缘猛转时降低。 |
| **`CONTROL_LINE_KD`** | **“误差变化有多快”**，使用相邻10 ms误差之差；灰度跳格也会被放大。 | KP稳定后从较小值开始；出现高频反向转向时降低或设0。 |
| **`CONTROL_LINE_ERROR_DEADBAND`** | 误差绝对值小于该范围时按0处理。 | 中线附近小幅抖动时增大；中线修正迟钝时减小。 |
| **`CONTROL_LINE_BASE_SPEED_CPS`** | 中线附近的基础前进命令。 | 调试时先低速，PD稳定后再逐步提高。 |
| **`CONTROL_LINE_MIN_TRACK_SPEED_CPS`** | 普通大偏差时允许降到的最低前进命令。 | 弯道冲出时降低；电机推不动车时提高。 |
| **`CONTROL_LINE_TURN_MAX_CPS`** | 普通循迹转向硬限幅；继续增大KP/KD也不能突破它。 | 转不过来时小步提高，转向过猛或跨过中线时降低。 |

**PD 调参顺序（放地上跑）：**

1. **降速起步**：`BASE_SPEED_CPS` 改成 1500，KD 改成 0
2. **只调 KP**：从 0.3 开始每次加 0.1，观察车在直线上有没有来回摆。出现蛇形（车位来回晃）就减 0.05。记下刚好不摆的 KP 值。
3. **加 KD**：KP 确定后，KD 从 0.05 加起。KD 加够时车在直线会非常稳，弯道入弯也更平滑。但如果 KD 加了反而更抖，说明传感器噪声大——退回 KD=0。
4. **提速**：逐步提高 `BASE_SPEED_CPS` 到 2000、2500、3000，每次提速后确认直线不摆、弯道能跟上。

#### 第四步：速度自适应参数

外环会根据误差绝对值自动减速：

```
误差 ≤ SPEED_FULL_ERROR  → BASE_SPEED_CPS
两阈值之间                 → 线性降速
误差 ≥ SPEED_MIN_ERROR   → MIN_TRACK_SPEED_CPS
```

| 参数 | 作用 | 怎么调 |
| --- | --- | --- |
| `CONTROL_LINE_SPEED_FULL_ERROR` | 误差在此范围内保持基础速度 | 中线附近不应减速时增大；希望更早减速时减小 |
| `CONTROL_LINE_SPEED_MIN_ERROR` | 误差达到此值后使用最低循迹速度 | 急弯来不及减速时减小 |
| `CONTROL_LINE_MIN_TRACK_SPEED_CPS` | 大偏差最低前进速度 | 弯道太慢时增大；弯道冲出时减小 |

#### 第五步：最外侧误差处理

独立`EDGE`边缘救线模式已经取消。最外侧传感器压线时仍使用同一套基础PD，并受`CONTROL_LINE_TURN_MAX_CPS`限幅；不会再强制最低转向量。

边缘转向过猛时优先降低`CONTROL_LINE_KP`或`CONTROL_LINE_TURN_MAX_CPS`；拉不回来时反向调整，并同时检查基础速度。

#### 第六步：丢线搜索参数

当所有传感器都看不到黑线时，当前基础版立即沿最后看到线路的方向原地找线，不进行左右交替扫描。

| 参数 | 作用 |
| --- | --- |
| `CONTROL_LINE_SEARCH_TURN_CPS` | 固定方向原地找线的转向量 |
| `CONTROL_LINE_SEARCH_TIMEOUT_MS` | 连续找不到线的停车超时 |

> 搜索参数**一般不调**。正常循迹不应该频繁丢线。如果经常丢线，先检查传感器阈值，再降低 `BASE_SPEED_CPS`。

#### 快速参考：常见现象和怎么改

| 现象 | 优先改什么 | 怎么改 |
| --- | --- | --- |
| 直线左右来回摆（蛇形） | KP 太大 / 没 KD | 减小 KP 0.05~0.1，或加 KD 0.05~0.1 |
| 直线很稳但弯道拐不过来 | KP 太小 | 加大 KP 0.1~0.2 |
| 入弯不减速直接冲出去 | DECEL_STEP 太小 / BASE_SPEED 太大 | 先降 BASE_SPEED，再加大 DECEL_STEP |
| 出弯后加速太慢 | ACCEL_STEP 太小 | 加大到 150~200 |
| 传感器明明看到线但 error 不对 | 阈值问题 | 重新校准白/黑采样 → MakeThreshold |
| 中线附近小幅高频抖动 | DEADBAND 太小 | 加大到 120~200 |
| 转向太猛、动作生硬 | SLEW_STEP 太小 | 加大 SLEW_STEP（奇怪吧？因为 SLEW 是限幅，加大=允许更快变化，但看起来更生硬。想要平滑就要减小 SLEW_STEP） |
| 弯道内侧轮居然反转了 | TURN_MAX 太大 | 代码限制 turn ≤ linear，但 linear 很小时 turn_max 也可能超过它。降低 TURN_MAX 或提高最低 linear 速度 |
| 频繁进入丢线搜索 | 传感器看不到线 | 先校准阈值，再降 BASE_SPEED |

> **PD参数关系**：`SPEED_FULL_ERROR < SPEED_MIN_ERROR`必须成立，否则速度计算会出错。搜索参数也有类似约束，见`Algorithm/control_config.h`注释。

### 1.3 常见现象与优先调节项

> ⚠️ **调试铁律：先内环后外环。** 车轮速度 PID（内环）没调好之前，循迹 PD（外环）怎么调都是白费。如果某个轮子一直跟不上目标，问题出在速度环，**不要用外环的 KP/KD 去补偿**。

| 现象 | 优先检查/调节 | 说明 |
| --- | --- | --- |
| **速度环问题**（现象发生在单个轮子上） | | |
| 左右轮在相同目标下实际速度差异明显 | 先查电机、轮胎、供电和机械阻力 | 当前没有车轮速度闭环，编码器反馈只用于判断差异，不能自动补偿 |
| 轮速稳定但总低于目标（如目标2000，稳定在1500） | 先检查输出是否长期达到`800‰`，再基于当前`KP=1.2、KI=0.08`小步调整 | 输出已饱和时继续加PID不会提高电机物理能力 |
| 轮子空载正常，放地上就明显变慢 | 检查供电压降、机械负载和PWM是否饱和；未饱和时再小幅增加KP | 当前内环已有积分，不能再按“KI为0”的旧结论处理 |
| 起步那一下轮子猛冲，然后降回来 | 减小 KI，或减小 `INTEGRAL_MAX` | 积分在停车期间攒了太多（虽然 `Chassis_Stop()` 会调 `PID_Reset()`，但如果没走到 Stop 而是突然降目标，积分不会清零） |
| 两侧轮子行为明显不同 | **对调电机/编码器排查硬件**，确认齿轮间隙和轴承顺滑度 | 不要用不同的 PID 参数去凑硬件差异 |
| **循迹外环问题**（现象发生在整车轨迹上） | | |
| 中线附近连续小幅摆动 | 适当增大 `CONTROL_LINE_ERROR_DEADBAND`，再检查 `CONTROL_LINE_KP`、`CONTROL_LINE_KD` | 先排除阈值噪声和左右传感器顺序错误 |
| 偏离后拐大弯并冲出线 | 降低`CONTROL_LINE_BASE_SPEED_CPS`或`CONTROL_LINE_MIN_TRACK_SPEED_CPS`，再调`CONTROL_LINE_KP`和转向上限 | 当前没有输出斜坡，大偏差后的速度和转向会直接变化 |
| 大误差时速度很低且突然急拐 | 提高`CONTROL_LINE_MIN_TRACK_SPEED_CPS`或降低`CONTROL_LINE_KP`、`CONTROL_LINE_TURN_MAX_CPS` | 当前最外侧也使用基础PD，没有独立边缘参数 |
| 已经到最外侧但拉不回来 | 先检查误差方向，再提高`CONTROL_LINE_KP`或`CONTROL_LINE_TURN_MAX_CPS`，必要时降低基础速度 | 不再存在强制最低救线转向 |
| 丢线后原地转向太弱或太强 | 调节`CONTROL_LINE_SEARCH_TURN_CPS` | 当前固定沿最后方向搜索，不进行左右扩大扫描 |
| 偶尔一帧丢线就进入搜索 | 当前没有丢线确认参数 | 需要先检查阈值和灰度稳定性；若仍需要应另行增加多帧确认逻辑 |
| 找到线后又立刻丢失 | 降低搜索强度并检查阈值 | 当前重新看到线后立即恢复PD，没有重新捕获确认参数 |

### 1.4 里程和编码器航向几何参数

`DRV_ENCODER_WHEEL_DIAMETER_MM` 在 `Driver/drv_encoder.h`。

`HEADING_ENCODER_WHEEL_BASE_MM` 在 `Algorithm/heading_estimator.h`（仅 `HEADING_SOURCE_ENCODER` 模式使用；当前默认 `HEADING_SOURCE_FUSED`）。

| 现象 | 优先改哪个 | 怎么改 |
| --- | --- | --- |
| 直走不够，例如目标 500，实际 450 | `DRV_ENCODER_WHEEL_DIAMETER_MM` | 调小 |
| 直走过头，例如目标 500，实际 550 | `DRV_ENCODER_WHEEL_DIAMETER_MM` | 调大 |
| 编码器航向模式下转角偏小 | `HEADING_ENCODER_WHEEL_BASE_MM` | 调大 |
| 编码器航向模式下转角偏大 | `HEADING_ENCODER_WHEEL_BASE_MM` | 调小 |
| 轮子转一圈 total 不是参数值 | `DRV_ENCODER_COUNTS_PER_REV` | 改成实测平均值 |

## 二、动作库参数（同样位于 Algorithm/control_config.h）

### 2.1 大白话解释

动作库做两件事：**直行指定距离** 和 **原地转指定角度**。它不直接控制 PWM，而是调用你刚调好的 `Chassis_SetSpeed()`，让底盘速度 PID（内环）来执行。所以 **内环必须先调好，再测动作库，否则距离和角度一定不准**。

动作的执行流程：

```
Motion_GoDistance(500mm, 1500cps)
  → Chassis_SetSpeed(±1500, 0)   // 直行，速度限幅在 [150, 4000]
  → 每 10ms：里程累加
  → 走了 ≥ (500 - 8)mm → 完成，停车

Motion_TurnAngle(90°)
  → 每 10ms：航向误差 × 25.0 = 转向速度，正常阶段限幅在 [1000, 3000]
  → Chassis_SetSpeed(0, 转向速度) // 原地差速转弯
  → 越过目标后：按误差反方向以 500cps 低速回正
  → 误差 ≤ 3° 持续 10 次采样（约100ms）→ 完成，停车
```

### 2.2 各参数详解

| 参数 | 当前值 | 建议值 | 通俗解释 |
| --- | ---: | ---: | --- |
| `CONTROL_MOTION_DISTANCE_TOLERANCE_MM` | `8` | **保持 `8`** | **”走到差多少算到”**。目标 500mm，实际走到 492mm 就判定完成。设太小（比如 1~2mm）会因为惯性永远到不了，设太大（比如 50mm）精度太差。8mm 大概是一个轮胎编码器 2~3 个计数的距离。 |
| `CONTROL_MOTION_ANGLE_TOLERANCE_DEG` | `3.0f` | **保持 `3.0f`** | **”转到差多少度算到”**。角度误差 ≤ 3° 时开始”稳定确认”。设太小（< 1°）容易来回修正卡住，设太大（> 5°）转角不准。3° 对小车来说肉眼几乎看不出来。 |
| `CONTROL_MOTION_DEFAULT_TIMEOUT_MS` | `8000U` | **保持 `8000`** | **”最多等多久”**。一个动作超过 8 秒还没完成就报错停车。500mm 以 1000cps 跑大概 1~2 秒，8 秒绰绰有余。如果车重、地滑或者目标距离很长，可以加大。 |
| `CONTROL_MOTION_MIN_ABS_SPEED_CPS` | `150` | **保持 `150`** | **”直行最慢速度”**。不管上层给多小的速度参数，实际发给底盘的不会低于 150cps。防止速度太低电机推不动。 |
| `CONTROL_MOTION_MAX_ABS_SPEED_CPS` | `4000` | **保持 `4000`** | **”动作速度总上限”**。直行动作传入的速度和统一转弯速度都会受此限制；低于底盘 `TARGET_MAX_CPS=5000`。 |
| `CONTROL_MOTION_TURN_SPEED_CPS` | `3000` | **统一使用** | **”定角转弯速度上限”**。测试、Basic和HJduino等入口调用`Motion_TurnAngle()`时不再各自传速度，全部共用此值。 |
| `CONTROL_MOTION_TURN_MIN_SPEED_CPS` | `1000` | **先保持 `1000`** | **”转弯最慢速度”**。即使角度误差很小（比如只剩 10°），转向速度也不会低于 1000cps。**这个参数最关键**——太大过冲严重、太小转不动。测试时如果每次转弯都转过头很多，优先减小这个值。 |
| `CONTROL_MOTION_TURN_CORRECTION_SPEED_CPS` | `500` | **先保持 `500`** | **”转过头以后往回修的速度”**。首次越过目标后锁定低速回正模式，方向始终跟随角度误差，直到重新进入±3°。太低可能克服不了静摩擦，太高可能再次跨过目标。 |
| `CONTROL_MOTION_TURN_KP_CPS_PER_DEG` | `25.0f` | **保持 `25.0f`观察** | **”角度差多少，转多快”**。误差90°时计算得到2250cps，未达到统一上限3000cps；误差低于40°时会受1000cps最小速度限制。 |
| `CONTROL_MOTION_TURN_SETTLE_SAMPLES` | `10U` | **保持 `10`** | **”稳定多久才算到”**。角度误差连续10次（每次10ms，共约100ms）都在±3°内才完成；若停车后因惯性越界，计数会清零并继续回正。 |

### 2.3 关键：理解转向速度的计算

以 `Motion_TurnAngle(90°)` 为例，实际输出速度是这样算的：

```
统一速度上限 = 3000
→ Motion_LimitSpeed: 限幅到 [150, 4000] → 还是 3000

每 10ms 实际转向速度：
  误差 90° → 90×25.0 = 2250cps
  误差 60° → 60×25.0 = 1500cps
  误差 30° → 30×25.0 = 750 → 低于 TURN_MIN → 取 1000cps
  误差 5°  → 5×25.0  = 125 → 低于 TURN_MIN → 取 1000cps
  误差 2°  → ≤ 容差 3° → 停车+稳定确认
  越过目标且误差反号 → 锁定500cps低速回正，方向随误差改变
```

**结论：正常转向阶段由KP、1000cps最小速度和3000cps统一上限共同决定；首次越过目标后不再使用1000cps最小速度，而是锁定500cps低速回正。** 这保证不同路线入口使用完全相同的定角控制，也避免转过头后高速反向修正。

### 2.4 推荐测试步骤

**第 1 步：直行距离测试**
- `Motion_GoDistance(300, 1000)` — 走 300mm，1000cps
- 实际测量走了多远，对比 `CONTROL_MOTION_DISTANCE_TOLERANCE_MM` 容差是否合理
- 如果总是差很多（比如目标 300 实际 250），先检查 `DRV_ENCODER_WHEEL_DIAMETER_MM` 和 `DRV_ENCODER_COUNTS_PER_REV`，**不是调动作库参数**

**第 2 步：转角测试（核心）**
- `Motion_TurnAngle(90)` — 按统一参数转 90°
- 在地上放一张纸，画好起始线和 90° 标记，看实际转了多少
- 如果明显转过头（比如转了 100°+），先观察是否会以500cps自动回到±3°；初次过冲太大可降低`TURN_MIN_SPEED_CPS`，回正仍越界则降低`TURN_CORRECTION_SPEED_CPS`
- 如果明显转不够（比如只转了 80°）：**增大 `TURN_MIN_SPEED_CPS`**，或者检查 IMU 航向是否正确
- 如果转完后来回晃很久才停下：**增大 `TURN_SETTLE_SAMPLES`** 到 5~8

**第 3 步：不同角度验证**
- 90° 调好后，试 45°、180°、360°
- 小角度（45°）主要验证 `TURN_MIN_SPEED_CPS` 是否合适
- 大角度（180°+）验证 `TURN_KP_CPS_PER_DEG` — 大弯应该明显比小弯快

### 2.5 快速参考：常见问题和怎么改

| 现象 | 优先改什么 | 怎么改 |
| --- | --- | --- |
| 走 500mm 实际只走了 400mm | 里程标定，不是动作库参数 | 调 `DRV_ENCODER_WHEEL_DIAMETER_MM` 和 `COUNTS_PER_REV` |
| 走 500mm 走了 550mm，但每次都很稳定 | 里程标定 | 同上 |
| 转 90° 首次超过100°但会返回 | 回正逻辑正常 | 观察最终是否稳定在±3°；需要减小初次过冲时再降低`TURN_MIN_SPEED_CPS` |
| 越过目标后无法往回移动 | `TURN_CORRECTION_SPEED_CPS` 太小 | 从500开始小幅增加，直到能克服静摩擦 |
| 低速回正仍反复跨过目标 | `TURN_CORRECTION_SPEED_CPS` 太大 | 每次降低50～100cps |
| 转 90° 每次只转了 ~70° | `TURN_MIN_SPEED_CPS` 太小 | 加到 1200，可能静摩擦太大推不动 |
| 转完后在原地小幅度来回摆动很久 | `TURN_SETTLE_SAMPLES` 太少 | 加到 5~8，让稳定确认更严格 |
| 动作执行到一半报 Error | 超时 | 检查 `DEFAULT_TIMEOUT_MS` 是否够；或者距离设太大、速度设太小 |
| 大角度转弯太慢 | `TURN_KP_CPS_PER_DEG` 太小 | 小幅增加，但最终不超过统一的`TURN_SPEED_CPS`上限 |
| 直行动作方向反了（让前进结果后退）| 编码器方向 | 检查 `DRV_ENCODER_WHEEL_DIR` 或电机接线 |

## 三、当前代码中的主要引脚（含驱动模式条件）

| 模块 | 功能 | STM32 引脚 | 外设/通道 | 代码位置/宏 | 说明 |
| --- | --- | --- | --- | --- | --- |
| USART1 可选链路 | USART1_TX | PA9 | USART1_TX | BSP/bsp_uart.h | 普通模式接 USB-TTL RX；E220 模式接 E220 RXD |
| USART1 可选链路 | USART1_RX | PA10 | USART1_RX | BSP/bsp_uart.h | 普通模式接 USB-TTL TX；E220 模式接 E220 TXD |
| E220 无线串口 | AUX | PE6 | 普通 GPIO 输入 / BSP_GPIO_E220_AUX | BSP/bsp_gpio.h | E220 模式启用；低电平忙、高电平空闲；板级引出情况待确认 |
| K210 串口 | USART2_TX | PA2 -> USART2_TX | USART2_TX | BSP/bsp_uart.h | 已启用，保持用于 K210/视觉 |
| K210 串口 | USART2_RX | PA3 -> USART2_RX | USART2_RX | BSP/bsp_uart.h | 已启用，保持用于 K210/视觉 |
| 电机 PWM | 左前 FL PWM | PC6 | TIM8_CH1 | BSP_PWM_CH1 | 对应 MOTOR_FL |
| 电机 PWM | 右前 FR PWM | PC7 | TIM8_CH2 | BSP_PWM_CH2 | 对应 MOTOR_FR |
| 电机 PWM | 左后 RL PWM | PC8 | TIM8_CH3 | BSP_PWM_CH3 | 仅 4WD 启用；默认 2WD 关闭 |
| 电机 PWM | 右后 RR PWM | PC9 | TIM8_CH4 | BSP_PWM_CH4 | 仅 4WD 启用；默认 2WD 关闭 |
| 电机方向 | FL_IN1 | PD0 | GPIO_OUT | BSP_GPIO_CH3 | 左前电机方向脚 1 |
| 电机方向 | FL_IN2 | PD1 | GPIO_OUT | BSP_GPIO_CH4 | 左前电机方向脚 2 |
| 电机方向 | FR_IN1 | PD2 | GPIO_OUT | BSP_GPIO_CH5 | 右前电机方向脚 1 |
| 电机方向 | FR_IN2 | PD3 | GPIO_OUT | BSP_GPIO_CH6 | 右前电机方向脚 2 |
| 电机方向 | RL_IN1 | PD4 | GPIO_OUT | BSP_GPIO_CH7 | 仅 4WD 启用；默认 2WD 关闭 |
| 电机方向 | RL_IN2 | PD7 | GPIO_OUT | BSP_GPIO_CH8 | 仅 4WD 启用；默认 2WD 关闭 |
| 电机方向 | RR_IN1 | PD8 | GPIO_OUT | BSP_GPIO_CH9 | 仅 4WD 启用；默认 2WD 关闭 |
| 电机方向 | RR_IN2 | PD9 | GPIO_OUT | BSP_GPIO_CH10 | 仅 4WD 启用；默认 2WD 关闭 |
| 编码器 | FL_ENC_A | PE9 | TIM1_CH1 | BSP/bsp_encoder.h | 左前编码器 A 相 |
| 编码器 | FL_ENC_B | PE11 | TIM1_CH2 | BSP/bsp_encoder.h | 左前编码器 B 相 |
| 编码器 | FR_ENC_A | PA0 | TIM2_CH1 | BSP/bsp_encoder.h | 右前编码器 A 相 |
| 编码器 | FR_ENC_B | PA1 | TIM2_CH2 | BSP/bsp_encoder.h | 右前编码器 B 相 |
| 编码器 | RL_ENC_A | PA6 | TIM3_CH1 | BSP/bsp_encoder.h | 仅 4WD 启用；默认 2WD 关闭 |
| 编码器 | RL_ENC_B | PA7 | TIM3_CH2 | BSP/bsp_encoder.h | 仅 4WD 启用；默认 2WD 关闭 |
| 编码器 | RR_ENC_A | PB6 | TIM4_CH1 | BSP/bsp_encoder.h | 仅 4WD 启用；默认 2WD 关闭 |
| 编码器 | RR_ENC_B | PB7 | TIM4_CH2 | BSP/bsp_encoder.h | 仅 4WD 启用；默认 2WD 关闭 |
| 灰度传感器 | 4051_OUT / SIG / AO | PC0 | ADC1_IN10 | BSP_ADC_CH1 | 仅 GRAY_SENSOR_SOURCE_4051 模式使用；当前默认 MCU-I2C 模式 |
| 灰度传感器 | 4051_S0 / A | PD10 | GPIO_OUT | BSP_GPIO_GRAY_S0 | 仅 GRAY_SENSOR_SOURCE_4051 模式使用；当前默认 MCU-I2C 模式 |
| 灰度传感器 | 4051_S1 / B | PD11 | GPIO_OUT | BSP_GPIO_GRAY_S1 | 仅 GRAY_SENSOR_SOURCE_4051 模式使用；当前默认 MCU-I2C 模式 |
| 灰度传感器 | 4051_S2 / C | PD12 | GPIO_OUT | BSP_GPIO_GRAY_S2 | 仅 GRAY_SENSOR_SOURCE_4051 模式使用；当前默认 MCU-I2C 模式 |
| 灰度传感器 | 4051_EN / INH | GND | 非 MCU 引脚 | 代码未控制 | 仅 GRAY_SENSOR_SOURCE_4051 模式使用；当前默认 MCU-I2C 模式 |
| I2C | I2C1_SCL | PB8 | I2C1_SCL | BSP/bsp_i2c.h | I2C1 共享：MCU 灰度、OLED、VL53L1X |
| I2C | I2C1_SDA | PB9 | I2C1_SDA | BSP/bsp_i2c.h | I2C1 共享：MCU 灰度、OLED、VL53L1X |
| SPI | LCD_CS | PB12 | GPIO_OUT | BSP_GPIO_LCD_CS | LCD 独立片选；2WD/4WD 均使用 |
| SPI | SPI2_SCK | PB13 | SPI2_SCK | BSP/bsp_spi.h | ICM-20948；4WD 时 LCD 也共用 SPI2 |
| SPI | SPI2_MISO | PB14 | SPI2_MISO | BSP/bsp_spi.h | ICM-20948；4WD 时 LCD 也共用 SPI2 |
| SPI | SPI2_MOSI | PB15 | SPI2_MOSI | BSP/bsp_spi.h | ICM-20948；4WD 时 LCD 也共用 SPI2 |
| 按键 | KEY1 | PE4 | GPIO_IN | BSP_KEY1 | 已启用，低电平按下 |
| 按键 | KEY2 | PE3 | GPIO_IN | BSP_KEY2 | 已启用，低电平按下 |
| 按键 | KEY3 | PE2 | GPIO_IN | BSP_KEY3 | 已启用，低电平按下 |
| 按键 | KEY4 | PE1 | GPIO_IN | BSP_KEY4 | 已启用，低电平按下 |
| 按键 | KEY5 | PA15 | GPIO_IN | BSP_KEY5 | 已启用；暂按内部上拉、低电平按下配置，板级电路待确认；占用 JTDI，但不影响 PA13/PA14 两线 SWD |
| 调试输出 | LED / 普通 GPIO | PC13 | GPIO_OUT | BSP_GPIO_CH1 | 默认调试 LED 模板 |
| 调试接口 | SWDIO | PA13 | SWD | 系统调试口 | 保留，不要占用 |
| 调试接口 | SWCLK | PA14 | SWD | 系统调试口 | 保留，不要占用 |

按键资源说明：PE1～PE4 和 PA15 在当前工程中都只配置为普通 GPIO 输入，不会启用或独占定时器。PE1 可复用 FSMC_NBL1/DCMI_D3；PE2～PE4 可复用跟踪、FSMC、以太网或 DCMI 信号；PA15 可复用 JTDI、TIM2_CH1/ETR、SPI1_NSS、SPI3_NSS/I2S3_WS。当前工程未在这些引脚上启用上述复用功能；TIM2 编码器的 CH1/CH2 已使用 PA0/PA1。使用 KEY5 后不能使用包含 JTDI 的完整 JTAG，烧录和调试应继续使用 PA13/PA14 两线 SWD。仓库没有开发板原理图，PA15 按键的实际有效电平待确认；若按下没有事件而松开/上电状态异常，应先核对硬件，再调整 `BSP_KEY5_PUPD` 和 `BSP_KEY5_ACTIVE_LEVEL`。

## 四、条件启用或默认关闭的复用资源

| 模块 | 功能 | STM32 引脚 | 外设/通道 | 默认状态 | 注意 |
| --- | --- | --- | --- | --- | --- |
| UART3 | TX | PB10 | USART3_TX | 关闭 | 和 I2C2_SCL 共用，启用前重查冲突 |
| UART3 | RX | PB11 | USART3_RX | 关闭 | 和 I2C2_SDA 共用，启用前重查冲突 |
| UART4 | TX | PA0 | UART4_TX | 关闭 | 已被编码器 FR_ENC_A 占用 |
| UART4 | RX | PA1 | UART4_RX | 关闭 | 已被编码器 FR_ENC_B 占用 |
| UART5 | TX | PC12 | UART5_TX | 关闭 | 和 SPI3_MOSI 共用 |
| UART5 | RX | PD2 | UART5_RX | 关闭 | 已被 FR_IN1 电机方向脚占用 |
| USART6 | TX | PC6 | USART6_TX | 关闭 | 已被 FL_PWM 占用 |
| USART6 | RX | PC7 | USART6_RX | 关闭 | 已被 FR_PWM 占用 |
| SPI1 | SCK | PA5 | SPI1_SCK | 2WD 启用；4WD 关闭 | 2WD 用于 LCD；4WD 时 PA6/PA7 被后编码器占用 |
| SPI1 | MISO | PA6 | SPI1_MISO | 2WD 启用；4WD 关闭 | 2WD 用于 LCD；4WD 时 PA6/PA7 被后编码器占用 |
| SPI1 | MOSI | PA7 | SPI1_MOSI | 2WD 启用；4WD 关闭 | 2WD 用于 LCD；4WD 时 PA6/PA7 被后编码器占用 |
| SPI3 | SCK | PC10 | SPI3_SCK | 关闭 | PC10 当前用于 LCD_DC，不是空闲 SPI3 引脚 |
| SPI3 | MISO | PC11 | SPI3_MISO | 关闭 | PC11 当前用于 LCD_BL，不是空闲 SPI3 引脚 |
| SPI3 | MOSI | PC12 | SPI3_MOSI | 关闭 | 和 UART5_TX 共用 |
| I2C2 | SCL | PB10 | I2C2_SCL | 关闭 | 和 USART3_TX 共用 |
| I2C2 | SDA | PB11 | I2C2_SDA | 关闭 | 和 USART3_RX 共用 |
| I2C3 | SCL | PA8 | I2C3_SCL | 关闭 | 备用 |
| I2C3 | SDA | PC9 | I2C3_SDA | 关闭 | 4WD 与 RR_PWM 冲突；2WD 下仍需核对其他复用 |
| ADC备用 | ADC_CH2 | PC1 | ADC1_IN11 | 关闭 | 备用模拟输入 |
| ADC备用 | ADC_CH3 | PC2 | ADC1_IN12 | 关闭 | 备用模拟输入 |
| ADC备用 | ADC_CH4 | PC3 | ADC1_IN13 | 关闭 | 备用模拟输入 |
| ADC备用 | ADC_CH5 | PC4 | ADC1_IN14 | 关闭 | 备用模拟输入 |
| ADC备用 | ADC_CH6 | PC5 | ADC1_IN15 | 关闭 | 备用模拟输入 |
| ADC备用 | ADC_CH7 | PA6 | ADC1_IN6 | 关闭 | 2WD 用作 SPI1_MISO；4WD 用作 RL_ENCODER_A |
| ADC备用 | ADC_CH8 | PA7 | ADC1_IN7 | 关闭 | 2WD 用作 SPI1_MOSI；4WD 用作 RL_ENCODER_B |
| EXTI模板 | EXTI_CH1 | PE0 | EXTI0 | 启用 | BSP_EXTI_CH1_ENABLE=1；PE0 为当前通用外部中断输入 |
| EXTI模板 | EXTI_CH2 | PE1 | EXTI1 | 关闭 | 已被 KEY4 占用 |
| EXTI模板 | EXTI_CH3 | PE2 | EXTI2 | 关闭 | 已被 KEY3 占用 |

## 五、需要额外确认或尚未完整分配的信号

| 模块 | 需要的信号 | 当前状态 | 建议 |
| --- | --- | --- | --- |
| 电机驱动板 | STBY / EN / SLP | 未分配；硬件连接待确认 | 如果驱动板有这个脚，建议后面新增一个 GPIO 控制，或者先硬件拉高/拉低到有效电平 |
| SPI 屏幕 | SCK/MOSI/MISO/CS/DC/RES/BL | CS=PB12、DC=PC10、BL=PC11；RES/RST 未分配 | 2WD 使用 SPI1；4WD 当前改用 SPI2 与 ICM 共线，联合验证前建议关闭 LCD_UI_ENABLE |
| 多个 SPI 设备 | 多个 CS | LCD 使用 PB12，ICM-20948 使用 PE5 | 新增 SPI 设备时必须分配独立 CS，并核对总线模式和时序 |
| IMU | I2C 或 SPI | 已实现：SPI2，CS=PE5 | 当前驱动使用 SPI2；4WD 下与 LCD 共线需联合验证 |
| ToF 测距 | I2C | 已实现：I2C1，7 位地址 0x29；XSHUT 默认未启用 | 与 MCU 灰度和 OLED 共用 I2C1；注意地址和总线负载 |
| 无线串口 E220 | UART + AUX | 已实现于 USART1：PA9/PA10，AUX=PE6，115200 8N1；当前默认启用 | PE6 在源码层无冲突，但开发板是否引出/复用待确认；M0、M1 需硬件可靠接低电平 |
| K210 / 视觉模块 | UART | 通信模块已实现但默认任务表未注册；协议仍需统一 | 当前 USART2 为 PA2/PA3；K210/main.py 与 STM32 帧格式不一致 |

## 六、LCD 硬件 SPI 接线（当前代码未分配独立 RES/RST 引脚）

LCD界面参数位于`APP/lcd_ui.h`：`LCD_UI_ENABLE=1U`启用界面，`LCD_UI_UPDATE_PERIOD_MS=200U`设置数据页刷新间隔，`LCD_UI_BOOT_HOLD_MS=1000U`设置启动页保持时间。`Test_ChassisCmd_Update()`可激活`CHASSIS PWM TEST`页面；`Test_RouteLog`激活LCD/OLED路线页面，其中`RAW`和`FIL`分别为灰度原始误差和滤波误差。OLED参数位于`APP/oled_ui.h`，路线数据分A/B两页自动切换；两块屏都必须注册各自的周期刷新任务。

| LCD 引脚 | STM32F407 接线 | 说明 |
| --- | --- | --- |
| GND | GND | 必须共地 |
| VCC | 3.3V | 按 LCD 模块规格确认；项目资料建议 3.3V |
| SCL / CLK / SCK | 2WD：PA5（SPI1）；4WD：PB13（SPI2） | SPI1_SCK    SPI2 |
| SDA / DIN / MOSI | 2WD：PA7（SPI1）；4WD：PB15（SPI2） | SPI1_MOSI   SPI2 |
| SDO / MISO | 2WD：PA6（SPI1）；4WD：PB14（SPI2） | 如果屏幕没有此引脚则不接 |
| CS | PB12（BSP_GPIO_LCD_CS） | LCD片选 |
| DC / A0 | PC10（BSP_GPIO_LCD_DC） | 命令/数据选择 |
| BL / BLK / LED | PC11（BSP_GPIO_LCD_BL） | 背光控制 |

## 七、陀螺仪硬件接线

| ICM-20948 模块 | STM32F407 |
| --- | --- |
| SCLK / SCL | PB13 |
| SDO / AD0 | PB14 |
| SDI / SDA | PB15 |
| CS / nCS | PE5 |
| GND | GND |
| INT | 暂时不接 |
| FSYNC | 暂时不接 |

## 八、云台加激光部分硬件连接

| 功能         | 引脚      | 外设        |
| ---------- | ------- | --------- |
| 水平舵机       | PF8     | TIM13_CH1 |
| 俯仰舵机       | PF9     | TIM14_CH1 |
| 激光开关       | PG2     | 普通推挽GPIO  |

## 九、K210 硬件连接

K210：STM32 使用 USART2（PA2 TX、PA3 RX，115200 8N1）；K210 侧 IO8 为 TX、IO6 为 RX，双方需共地。当前 K210/main.py 与 STM32 的数据帧格式不一致，联调前必须统一协议。

## 十、E220-400T22D 硬件连接

| E220 引脚 | STM32F407 | 说明 |
| --- | --- | --- |
| RXD | PA9 / USART1_TX | STM32 向 E220 发送数据 |
| TXD | PA10 / USART1_RX | STM32 接收 E220 输出数据 |
| AUX | PE6 / BSP_GPIO_E220_AUX | 普通 GPIO 输入，无上下拉；低忙高闲；板级引出情况待确认 |
| M0 | GND | 透明传输模式要求低电平，不可悬空 |
| M1 | GND | 透明传输模式要求低电平，不可悬空 |
| GND | GND | 必须共地 |

USART1 保持 115200、8 数据位、无校验、1 停止位、无硬件流控。AUX 是模块输出，只需要普通 GPIO 输入，不需要定时器、ADC 或专用中断脚；本实现采用轮询。严禁把 PE6 配成输出低电平，也不要给 AUX 外接下拉，否则模块上电时可能被强制带入升级模式。

### （一）改变模块配置

改变E220-400T22D配置方法：打开E220  V1.3M0 M1接3.3，RX TX接USB转TTL，VCC GND正常接，USB转TTL接入电脑

配置时，M0、M1 临时接 3.3V 进入配置模式；RXD 接 USB-TTL 的 TX，TXD 接 USB-TTL 的 RX，VCC、GND 正常供电并与 USB-TTL 共地，USB-TTL 的 IO 电平必须为 3.3V。修改完成后先断电，再将 M0、M1 可靠接地，重新上电进入透明传输模式。收发两端的信道、地址、网络 ID、空中速率等无线参数必须匹配，串口参数保持 115200、8N1；具体配置项以所用固件和 E220 V1.3 配置软件实际显示为准。
